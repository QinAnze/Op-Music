{
  "identifier": "default",
  "description": "Default capabilities for Op Music",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "fs:default",
    "fs:allow-read",
    "fs:allow-exists",
    "fs:scope",
    "dialog:default",
    "dialog:allow-open",
    "shell:default"
  ]
}
